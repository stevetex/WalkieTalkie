import SwiftUI

/// Placeholder until the ring, answer and conversation flow is ported from the spike.
struct ContentView: View {
    var body: some View {
        VStack(spacing: 4) {
            Text("Over&Out")
                .font(.title3.bold())
            Text("Walkie-talkie")
                .foregroundStyle(.secondary)
        }
    }
}

#Preview {
    ContentView()
}
